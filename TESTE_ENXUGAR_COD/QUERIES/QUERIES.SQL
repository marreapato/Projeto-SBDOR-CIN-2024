SELECT C.ID_COMPRA,C.DATA_COMPRA,DEREF(AL.PASSAGEM.PASSAGEIRO).PK_CPF,AL.PASSAGEM.PK_LOCALIZADOR_PASSAGEM FROM COMPRA_TB C, TABLE(C.PASSAGENS_COMPRADAS) AL
WHERE DEREF(AL.PASSAGEM.PASSAGEIRO).PK_CPF IN (
SELECT P.PK_CPF FROM PASSAGEIRO_TB P
WHERE P.CALCULAR_IDADE() BETWEEN 26 AND 30);
